<%= turbo_stream.update "order-link" do %>
  <%= link_to "#{SiteMemo.orders_i18n[@order_key.to_sym]}にする",
          inner_sashes_update_order_path(@inner_sash.id, params: { order: @order_key }),
          data: {turbo_method: :post, turbo_confirm: "#{SiteMemo.orders_i18n[@order_key.to_sym]}にしますか？"} %>
<% end %>
<%= turbo_stream_flash %>